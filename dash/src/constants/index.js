import { aadhaar } from "../assets"; // Make sure the path is correct

const Card = [
  {
    name: "Aadhaar Card",
    image: aadhaar, // Pass the imported image here
  },
  // Add other card objects if needed
];

export default Card;
